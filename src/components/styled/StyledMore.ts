import styled from 'styled-components';
import StyledContentHeader from "./StyledContentHeader";

const StyledMore = styled(StyledContentHeader)`
h2 {
  margin-bottom: 1.5rem;
  font-size: 1.5rem;
  font-weight: bold;
}
p {
  margin-bottom: 1rem;
}
`

export default StyledMore;